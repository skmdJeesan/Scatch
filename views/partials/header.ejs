<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="w-full min-h-screen relative">
    <nav class="w-full fixed flex justify-between px-5 py-3">
        <h3 class="text-xl font-bold text-blue-600">Scatch</h3>
        <% var display = typeof loggedin === 'undefined' ? true : loggedin %>
        <% if(display) { %>
            <div class="flex gap-7">
                <a href="/shop" class="text-sm cursor-pointer hover:text-blue-600 transition-transform">Shop</a>
                <a href="/cart" class="text-sm cursor-pointer hover:text-blue-600 transition-transform">Cart</a>
                <a href="/owners/login" class="text-sm cursor-pointer hover:text-blue-600 transition-transform">Create products</a>
                <a href="" class="text-sm cursor-pointer hover:text-blue-600 transition-transform">My Account</a>
                <a href="/users/logout" class="text-sm text-red-500 cursor-pointer hover:text-red-900">Log out</a>
            </div>
        <% } %>
    </nav>