<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="w-full min-h-screen relative">
    <nav class="w-full fixed flex justify-between px-5 py-3">
        <h3 class="text-xl font-bold text-blue-600">Scatch</h3>
        <% var display = typeof loggedin === 'undefined' ? true : loggedin %>
        <% if(display) { %>
            <div class="flex gap-4">
                <a href="/shop" class="">Shop</a>
                <a href="/cart" class="">Cart</a>
                <a href="" class="">My Account</a>
                <a href="/users/logout" class="text-red-500">Log out</a>
            </div>
        <% } %>
    </nav>